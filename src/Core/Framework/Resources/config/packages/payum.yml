payum:
    security:
        token_storage:
            Payum\Core\Model\Token:
                filesystem:
                    id_property: 'hash'
                    storage_dir: '%kernel.cache_dir%'
